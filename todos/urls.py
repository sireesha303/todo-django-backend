from .views import *
from django.urls import path

urlpatterns = [
    path('', todosList, name='todos_list'),
    path('add/', createTodo, name='add-todo'),
    path('<str:id>/', todoDetail, name='todo-details'),\
    path('<str:id>/update/', updateTodo, name="update-todo"),
    path('<str:id>/delete/', deleteTodo, name="delete-todo"),
]






































































































































































